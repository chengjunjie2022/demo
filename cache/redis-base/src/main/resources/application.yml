# 配置redis
spring:
  redis:
    host: 127.0.0.1
    port: 6379
#    password:
    timeout: 200
    database: 0
    block-when-exhausted: true
    lettuce:
      pool:
        max-active: 10
        max-wait: 200
        max-idle: 5
        min-idle: 1